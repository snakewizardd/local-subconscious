**To the project (lattice_explorer / Local Subconscious)**

The lock is already correct. A hit is \((ja)k=(Na)r\) with \(k,r\) even, i.e. \(2N\mid jk\), with period

\[
P_j=\frac{2N}{\gcd(j,2N)},
\]

stretched to \(2P_j\) when \(P_j\) is odd because \(k\) is forced even. That is the whole theorem. \(a\) never changes *who* hits. \(N\) changes the species of clock. \(K\) is how far you are willing to look, and a long \(K\) will manufacture coincidences.

**What I want the experiment to be**

Do not promote this on the strength of the city maps or a single civic \(N=24\).

A rich composite \(N\) (24, 60) is a selection machine. High-\(\gcd(j,2N)\) stacks surface often. Coprime stacks almost never surface inside a realistic horizon. That is a **temporal barycenter**: the same failure mode the spatial autopsy already named, now in time. Organic-style \(0.29\) pairs live on the starved spokes. If you resurface only on hits of one pretty clock, you will keep offering the Room its own commensurate friends.

So the test is not “does the lattice find alignments.” It will. The test is whether those alignments are doing useful work or just echoing the house factorisation.

Four arms, same usefulness score you already trust from write-time reflection (did the neighbor change the next write, or repeat the barycenter):

1. single-\(N\) hits (start with 24)  
2. multi-\(N\) consensus — only if \(j\) is an **absolute** quantized cadence, not a spoke of one world; a triple lock is \(\operatorname{lcm}(2N_i)\mid jk\), not a vibe  
3. **anti-hits** — resurface from the complement, the stacks this \(N\) refuses  
4. jittered / fixed interval  

(2) without (3) is incomplete. Consensus among composites can just mean “agrees with every clock we already liked.” Put a prime \(N\) in the set, or keep the complement arm.

**What I would still build, cheap**

- Emit \(P_j\) and the even-adjusted period next to each column.  
- Dual table: freeze \(k\), vary \(N\) — which worlds claim this radius.  
- Do not run \(N=60\) with \(K<120\); the map is a keyhole.

**Promotion rule (already in the README; keep it)**

Leave `misc` unless (2) or (3) beats (4) on that reflection score. The lattice is a good lock. It is not yet evidence that the Room should live on it.

## Addendum

Adopt from copilot_notes.md, do not re-derive:
- `Q_j = lcm(2, P_j)` is what the even-k code actually shows.
- `H_j(N,K)` / `E_j` exposure. A dark stack is starved or ignored — those are different.
- `W(j,k)` and `σ` on a heterogeneous panel. Dissent gets a budget, not only consensus.

Keep from this note:
- `j` in a multi-N test is an absolute quantized cadence, not a spoke of one world.
- A full-panel lock is `lcm(2N_i) | j k`, not a special aura. For `{24,29,35}` that modulus is `48720`.
- Anti-hit / complement is the bias instrument. Consensus among composites is not enough.
- Promotion still requires an arm that beats jitter on write-time reflection.

Do not merge the city skin into the test. The map is allowed. The clock is not in `process_thought` until the score moves.